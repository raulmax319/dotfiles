- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.gitconfig:
    ~/.zshrc:

- create:
    - ~/Developer
# - shell:
#  - [git submodule update --init --recursive, Installing submodules]
