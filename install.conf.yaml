- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.gitconfig:
    ~/.zshrc:

- create:
    - ~/Developer
- shell:
    - command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
