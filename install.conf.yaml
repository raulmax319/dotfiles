- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.gitconfig:
    ~/.zshrc:
    ~/OneDrive/ssh:

- create:
    - ~/Developer
- shell:
    - command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
